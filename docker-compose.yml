services:
  app:
    build:
      context: ./backend
    container_name: fleetify_app
    ports:
      - "8080:8080"
    volumes:
      - ./backend:/app
    depends_on:
      - mysql
    env_file:
      - .env

  mysql:
    image: mysql:8.0
    container_name: fleetify_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: fleetify
    ports:
      - "3307:3306"

  frontend:
    build:
      context: ./frontend
    container_name: fleetify_frontend
    ports:
      - "3000:80"
    depends_on:
      - app